
const HOST = process.env.NODE_ENV  !== 'production' ? '/api' : 'http://wechat.ngba.cn/api'                                                      
export default { 
    Hallowmas:HOST,
    //接口代理配置
  }
